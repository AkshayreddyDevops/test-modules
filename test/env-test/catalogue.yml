name: catalogue-data
schema:
  enabled: true
  container:
    master-data: master-data.sql
    app-user: app-user.sql
    schema: schema.sql
  env:
    - name: CART_ENDPOINT
      value: localhost


    

